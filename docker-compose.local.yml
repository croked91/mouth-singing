# Local override — mount media from external TOSHIBA drive
services:
  backend:
    volumes:
      - sqlite_data:/data/sqlite
      - "/media/croked/TOSHIBA EXT1/backups/volumes/media_data:/data/media"
